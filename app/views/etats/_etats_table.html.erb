<%= hidden_field_tag :direction, params[:direction] %>
<%= hidden_field_tag :sort, params[:sort] %>

<table class="table table-striped table-bordered table-condensed sortable">
    <thead>
        <tr>
            <th style="width: 200px;">Nom</th>
            <th style="width: 200px;">Description</th>
            <th style="width: 40px;"></th>
        </tr>
    </thead>
    <tbody>
        <% @etats.each do |etat| %>
        <tr>
            <td valign="top"> 
                <%= link_to etat.name, edit_etat_path(etat), :remote => true %> 
            </td>
            <td valign="top"> 
                <%= etat.description %> 
            </td>
            <td valign="top"> 
                <%= link_to edit_etat_path(etat), :remote => true, :style=>'height: 14px;', :title=>'Edit' do%> 
                    <i class="icon-edit"></i> 
                <% end %> 
                
                <a style="height: 14px; padding-left: 5px;" title="Delete"
                    data-backdrop="static" data-keyboard="true"
                    href="javascript:;"
                    onclick="show_confirm_modal('<%= etat_path(etat) %>',
                    'Are you sure you want to delete the etat : <%= etat.name%>');"> 
                    <i class="icon-remove"></i> 
                </a>
             </td>
        </tr>
        <% end %>
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3"> <%= will_paginate @etat%> </td>
        </tr>
    </tfoot>
</table>
